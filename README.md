# restaurant

## Start
Install npm modules:
```bash
$ yarn
```
or
```bash
$ npm i
```

Set config in `app/`:
- `db.json`
- `config.json`

Copy webview static file to `app/public/`

Start server:
```bash
$ npm start
```

